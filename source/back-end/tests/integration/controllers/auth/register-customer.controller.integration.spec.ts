describe('RegisterCustomerController - POST /api/auth/register', () => {
  it('should register a new customer successfully', async () => {
    // arrange

    // act

    // assert
  })

  it('should throw a bad request exception when invalid inputs', async () => {
    // arrange

    // act

    // assert
  })
})
